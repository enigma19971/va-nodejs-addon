{
  "name": "gs-addon",
  "version": "1.0.0",
  "description": "decode rtmp stream to images",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean"
  },
  "author": "Shawl",
  "license": "ISC",
  "dependencies": {
    "node-addon-api": "^6.1.0"
  }
}
